class Solution {
    public boolean isPalindrome(int x) {
        int a=x;
        int reverse=0;
        while(a>0){
            reverse=(reverse*10)+(a%10);
            a=a/10;
        }
        
            
        return reverse==x;
        
    }
}
